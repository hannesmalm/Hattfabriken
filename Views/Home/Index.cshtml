@model Hattfabriken.Models.IndexViewModel

@{
    ViewData["Title"] = "Home Page";
}

<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-3">
              @if (!User.Identity.IsAuthenticated)
               {
                <a asp-controller="Request" asp-action="NewRequest" class="btn btn-success w-100 shadow">Create order request</a>
               }
            </div>
        </div>
    </div>
</body>

<!-- CARDBODY FÖR HATTAR -->
<div id="hatList">
    <div class="rox mt-5">
        <div class="col">
            <div class="card mx-auto mb-4 shadow" style="width: 240px;">
               <img class="img roundes-circle m-4" style="aspect-ratio: 1/1; object-fit: cover;"
               src="" alt="Hat Picture"/>
               <div class="card-body text-center">
                   <h5 class="card-title">Hatt standardutbud</h5>
                   <p class="card-text">blablabla</p>
                   <div class="text-center">
                       <button class="btn btn-success mb-2">Order</button>
                   </div>
               </div>
            </div>
        </div>
    </div>
</div>

<div class="row justify-content-center">
    <a asp-controller="Admin" asp-action="Index" class="btn btn-primary">Go to Admin Page</a>
</div>

<div>
    <a asp-controller="Account" asp-action="CreateAccount">Skapa konto</a>
</div>

<div class="row justify-content-center">
    <a asp-controller="Mail" asp-action="SendEmail">Send an email</a>
</div>
<a id="openOutlook" href="" onclick="openOutlook()">Open Outlook</a>

<script>
    function openOutlook() {
        var emailBody = "Dear Customer,\n\nWe regret to inform you that your request has been denied. Unfortunately, we are unable to create the hat you have requested at this time.\n\nBest regards,\nThe Hat Factory Team";
        var encodedEmailBody = encodeURIComponent(emailBody);
        var mailtoLink = "mailto:?subject=Request%20Denied&body=" + encodedEmailBody;
        document.getElementById("openOutlook").setAttribute("href", mailtoLink);
    }
</script> 


